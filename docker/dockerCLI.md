# exec
```bash
docker exec -it openclaw-workshop-agent sh
```
run command inside running container

# run
```bash
docker run
```

creates a container

# -it
short flags combined
-i, interactive, keep stdin open, session can receive input, for shells and interactive programs
-t, tty, allocate pseudo-terminal