# an ide
split into two, one editor, one agent manager to spawn different agents
with built in headless chrome browser for automated ui testing

# cost
uses a lot of ram
but right now in preview tier, so free