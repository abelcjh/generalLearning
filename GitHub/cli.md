# extensions
```bash
gh extension install <extension repo shortcut>
```
default all extensions are global
extension repo name must have `gh-`  at the start, eg abelcjh/gh-agenticExtension

```bash
gh extension list
```
to see all extensions installed